#############################################
### Ignore the following files in the slug.
#############################################
# Run the following commands to see the 
# files included in the SLUG and their sizes:
# $ heroku run bash --app <heroku-app-name>
# $ ls -al
##############################################
target/*.jar.original
target/*.mvn-dependency-list.log
target/test-classes